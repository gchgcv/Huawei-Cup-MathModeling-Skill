# 文档目标：latex-working-draft

仅在用户**明确要求创建新的 LaTeX 文件/工作稿**，且没有现有/官方/用户模板时使用。若用户只要求修改、分析或给出 LaTeX 内容，不得据此自行创建新 `.tex`。

用户已指定目标文件时写入其指定目标；用户只授权“创建 LaTeX 工作稿”但未指定文件落点时，才允许以 `templates/latex/working-draft/main.tex` 作为模板来源。不得因此产生多个平行主稿，也不得临时另造不同 preamble。该模板只用于可编译工作稿，不是官方模板，不能直接支撑 READY_TO_SUBMIT。
